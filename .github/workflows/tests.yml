name: Tests
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install -r requirements-lock.txt
      - run: python -m pip install -e . --no-deps
      - run: python -m pytest -q
      - run: catalogue-guard --data-dir data-demo demo
      - run: catalogue-guard --data-dir data-demo stats
      - run: catalogue-guard --data-dir data-demo powerbi --output output/powerbi-ci
